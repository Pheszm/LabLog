#LABLOG STARTER. Run this Line by Line

CREATE database sqlandcsharp;

use sqlandcsharp;

CREATE TABLE studentlist(
    StudentID varchar(255),
    StudentName varchar(255),
    Course varchar(255),
    YearLevel varchar(255),
    Gender varchar(255)
);

CREATE TABLE subjectlist(
    Subjects varchar(255)
);

CREATE TABLE courselist(
    course varchar(255)
);

INSERT INTO courselist (course)
VALUES ("IT"),
("HM"),
("EDUC"),
("RVM-TTP"),
("BSBA"),
("CRIM");

CREATE TABLE instructors_account(
    Username varchar(255),
    Pass varchar(255)
);

INSERT INTO instructors_account (Username, Pass)
VALUES ("Razel", "123456"),
("Carl", "123456"),
("Marly", "123456");


Create table Logbooks(
	IPaddress varchar(200),
	FullName varchar(200),
    	ID varchar(100),
    	Purpose varchar(250),
   	Reason varchar(5000),
   	TimeIn varchar(50),
    	TimeOut varchar(50),
    	ExactDate varchar(50)
);


#sortnames:
SELECT * FROM studentlist ORDER BY StudentName;

#HOW TO FIX THIS ERROR?
MySql.Data.MySqlClient.MySqlException: 'Authentication to host 'localhost' for user 'root' using method 'mysql_native_password' failed with message: Access denied for user 'root'@'localhost' (using password: YES)'

#Run This:
SELECT user, host FROM mysql.user;

ALTER USER 'root'@'localhost' IDENTIFIED BY '123456';



#To give Access for Remoting the Database
CREATE USER 'root'@'%' IDENTIFIED BY '123456';

GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;

FLUSH PRIVILEGES;

